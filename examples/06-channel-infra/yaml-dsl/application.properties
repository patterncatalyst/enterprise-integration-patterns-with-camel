# Camel CLI — component configuration
kafka.brokers=localhost:9092
camel.component.pulsar.service-url=pulsar://localhost:6650

# PostgreSQL datasource (for channel-adapter SQL route)
camel.component.sql.dataSource.url=jdbc:postgresql://localhost:5432/eip
camel.component.sql.dataSource.user=eip
camel.component.sql.dataSource.password=eip
